function chunkArrayInGroups(arr, size) {
    //check
    if(arr.length === 0 ) {
        return "incorrect input"
    }

    var groups = [];
    while (arr.length > 0) {

    }

    return groups;
}
chunkArrayInGroups(["a", "b", "c", "d"], 2)